#include <iostream>
#include <vector>
#include <queue>
#include <algorithm>
#define fast cin.tie(0)->sync_with_stdio(0);

using namespace std;

using ll = long long;

/*
    plan : 
    R, B marble

    x   
*/

int main(){
    fast
    int n,m;
    vector<string>v(n);
    for(auto& iter : v)
        cin >> iter;

    
    return 0;
}